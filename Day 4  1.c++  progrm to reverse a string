#include <bits/stdc++.h>
using namespace std;
void rev(string str)
{
    for (int i=str.length() - 1;i >= 0;i--)
        cout << str[i];
}
int main(void)
{
    string s;
    cout<<"ENter a string:";
    cin>>s;
    rev(s);
    
}
